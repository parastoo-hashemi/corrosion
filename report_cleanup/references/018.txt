./Documentation/final_augmentation_strategy.md:6:> **Based on:** `Documentation/augmentation_journal_plan.md`, `Documentation/codex/augmentation_dataset_report.md`, `augmentation/augment_dataset.py`, pixel statistics measured from actual images.
./Documentation/final_augmentation_strategy.md:150:These transforms are scientifically defensible but are not included in the primary deliverable because they introduce additional complexity without a clear gain for the dataset-preparation stage. They belong in the training-time ablation experiments described in `augmentation_journal_plan.md`.
./Documentation/augmentation_methodology_review.md:137:The journal plan (`augmentation_journal_plan.md`), which is referenced in the report, contains the statement:
./Documentation/augmentation_methodology_review.md:436:**Source:** This appears in `augmentation_journal_plan.md` as an "expected shape of results" in the robustness experiment. It is stated as a present fact in the report but was never measured.
