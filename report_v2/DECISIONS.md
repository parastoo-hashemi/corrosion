# Report rebuild decisions

Date: 2026-09-16. Branch: `report-rebuild-codex`, created from clean `master`
at `1089b3e7f6c7f798a3bf5c6ccd07eb6c1fb3e322`.

1. **Scope.** The user explicitly authorized carrying out `activity_report/CODEX_AUTONOMOUS_PROMPT.md`. All new material lives in `report_v2/`; the frozen report and modelling source remain untouched. No model retraining, remote push, or merge is part of this run.
2. **Authorship.** Use Parastoo Hashemi Alvar and Politecnico di Torino, verified against the saved article source. Do not invent supervisor, degree-programme, submission date, or approval. Label the thesis as an MSc technical activity / thesis manuscript, not an officially submitted thesis. Version/date are editorial metadata.
3. **Evidence authority.** Saved prediction and metric files outrank summary prose. In particular, final wire-loss selection is metadata-only CatBoost (grouped MAE 0.123797), distinct from the diagnostic RandomForest result (0.105037). Some older summaries pair the CatBoost name with the RandomForest number; neither document will do so.
4. **Comparability.** Distinct representations reuse the same specimens; they are not independent datasets or replications. Earlier 792-row / five-category builds and the 791-row / four-category build are distinguished. Never rank their MAEs as a common leaderboard.
5. **Capacity estimand.** The refocused workflow attaches one terminal target per specimen to all training photographs and scores terminal held-out photographs. Thus its headline metric is terminal-load estimation at the terminal image, not prospective early-warning performance. Repeated target values do not increase the independent structural sample size. Training is observation-weighted; per-specimen weight equality is not implemented there.
6. **Selection and uncertainty.** Per-feature-set winners are selected using the same saved folds that are reported. These are exploratory model comparisons, not a nested estimate of the selection procedure. Fold sample SD is descriptive, not a confidence interval or a significance test. A tiny MAE difference is not evidence of equivalence. No new inferential p-values are introduced for model comparisons.
7. **LOCO.** Treat campaign exclusion as a severe extrapolation stress test. Mesh/chloride/duration have only two jointly observed campaign-level combinations. This does not isolate causal contributions. Re-selecting a model under LOCO cannot be presented as a fixed-model collapse ratio.
8. **Post-onset sensitivity.** The code uses the first total-rust reading >=0.5% and retains subsequent rows, including any later lower readings; this is not a universal human-visibility cutoff or a simple row-wise threshold. Its 43 terminal specimens and selected model differ from the pooled comparison. Describe a limited observed improvement, not a demonstrated exception or statistically established benefit.
9. **Software.** Confirm the YAML `NO` boolean defect and live `main_first` substitutions; leave them unchanged to preserve the report-only scope. Artifact mtimes support that the cited saved outputs predate the affected source timestamps, but mtimes alone do not prove the historical executable or absence of all earlier bugs. The mapping file itself is later and includes corrupted narrative fields; cited design fields remain intact. The later augmentation branch is separately checked clean.
10. **Publication formats.** Use installed IEEEtran conference class and IEEEtran numeric bibliography for the article; use a modular A4 report with numeric IEEEtran references for the thesis. No specific venue/page limit or institutional template was supplied. Preserve both v1 and v2 as complete source directories sharing figures, tables, and bibliography.
11. **Figures.** Review old figure critiques and replot selected results as vector PDFs with readable labels, units, and provenance. This replaces dense diagnostics and the old cross-phase charts; original verified result values are retained. Diagrams are explicit conceptual illustrations, not extra measurements.
12. **Literature.** Use predecessor references already in the repository and verified publisher/author records for related work. Record metadata and URLs in `report_v2/references/SOURCES.md`. The literature review is focused, not a systematic search or a claim of exhaustive novelty.
13. **Available data.** Raw images are in a local ZIP archive; verify all PNGs directly without expanding it. Do not imply that every original absolute training path is portable. The `emiling` exports are secondary copies; no separate `main_4_v2` directory exists in this checkout.
14. **Measurement definitions.** Independently read the predecessor thesis: four-point bending after ageing, section 4.2.2.3 (printed p.33); bottom longitudinal layer near the crack, section 4.2.3 (p.34); selected cutting-face area loss, section 5.2.1. Neither endpoint is a complete time-resolved internal damage field.
15. **Status.** The current four-class branch has prepared data and no trained classifier. Earlier five-class outputs and the external predecessor's three-class ResNet-50 work are distinct. New training is a next step, not part of this report rebuild.

Further decisions made during critique and visual QA are appended below.
