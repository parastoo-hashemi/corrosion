# Report rebuild decisions

Date: 2026-09-16. Branch: `report-rebuild-codex`, created from clean `master`
at `1089b3e7f6c7f798a3bf5c6ccd07eb6c1fb3e322`.

1. **Scope.** The user explicitly authorized carrying out `activity_report/CODEX_AUTONOMOUS_PROMPT.md`. All new material lives in `report_v2/`; the frozen report and modelling source remain untouched. No model retraining, remote push, or merge is part of this run.
2. **Authorship.** Use Parastoo Hashemi Alvar and Politecnico di Torino, verified against the saved article source. Do not invent supervisor, degree-programme, submission date, or approval. Label the thesis as an MSc technical activity / thesis manuscript, not an officially submitted thesis. Version/date are editorial metadata.
3. **Evidence authority.** Saved prediction and metric files outrank summary prose. In particular, final wire-loss selection is metadata-only CatBoost (grouped MAE 0.123797), distinct from the diagnostic RandomForest result (0.105037). Some older summaries pair the CatBoost name with the RandomForest number; neither document will do so.
4. **Comparability.** Distinct representations reuse the same specimens; they are not independent datasets or replications. Earlier 792-row / five-category builds and the 791-row / four-category build are distinguished. Never rank their MAEs as a common leaderboard.
5. **Capacity estimand.** The refocused workflow attaches one terminal target per specimen to all training photographs and scores terminal held-out photographs. Thus its headline metric is terminal-load estimation at the terminal image, not prospective early-warning performance. Repeated target values do not increase the independent structural sample size. Training is observation-weighted; per-specimen weight equality is not implemented there.
6. **Selection and uncertainty.** Per-feature-set winners are selected using the same saved folds that are reported. These are exploratory model comparisons, not a nested estimate of the selection procedure. Fold sample SD is descriptive, not a confidence interval or a significance test. A tiny MAE difference is not evidence of equivalence. No new inferential p-values are introduced for model comparisons.
7. **LOCO.** Treat campaign exclusion as a severe extrapolation stress test. Mesh/chloride/duration have only two jointly observed campaign-level combinations. This does not isolate causal contributions. Re-selecting a model under LOCO cannot be presented as a fixed-model collapse ratio.
8. **Post-onset sensitivity.** The code uses the first total-rust reading >=0.5% and retains subsequent rows, including any later lower readings; this is not a universal human-visibility cutoff or a simple row-wise threshold. Its 43 terminal specimens and selected model differ from the pooled comparison. Describe a limited observed improvement, not a demonstrated exception or statistically established benefit.
9. **Software.** Confirm the YAML `NO` boolean defect and live `main_first` substitutions; leave them unchanged to preserve the report-only scope. Artifact mtimes support that the cited saved outputs predate the affected source timestamps, but mtimes alone do not prove the historical executable or absence of all earlier bugs. The mapping file itself is later and includes corrupted narrative fields; cited design fields remain intact. The later augmentation branch is separately checked clean.
10. **Publication formats.** Use installed IEEEtran conference class and IEEEtran numeric bibliography for the article; use a modular A4 report with numeric IEEEtran references for the thesis. No specific venue/page limit or institutional template was supplied. Preserve both v1 and v2 as complete source directories sharing figures, tables, and bibliography.
11. **Figures.** Review old figure critiques and replot selected results as vector PDFs with readable labels, units, and provenance. This replaces dense diagnostics and the old cross-phase charts; original verified result values are retained. Diagrams are explicit conceptual illustrations, not extra measurements.
12. **Literature.** Use predecessor references already in the repository and verified publisher/author records for related work. Record metadata and URLs in `report_v2/references/SOURCES.md`. The literature review is focused, not a systematic search or a claim of exhaustive novelty.
13. **Available data.** Raw images are in a local ZIP archive; verify all PNGs directly without expanding it. Do not imply that every original absolute training path is portable. The `emiling` exports are secondary copies; no separate `main_4_v2` directory exists in this checkout.
14. **Measurement definitions.** Independently read the predecessor thesis: four-point bending after ageing, section 4.2.2.3 (printed p.33); bottom longitudinal layer near the crack, section 4.2.3 (p.34); selected cutting-face area loss, section 5.2.1. Neither endpoint is a complete time-resolved internal damage field.
15. **Status.** The current four-class branch has prepared data and no trained classifier. Earlier five-class outputs and the external predecessor's three-class ResNet-50 work are distinct. New training is a next step, not part of this report rebuild.

Further decisions made during critique and visual QA are appended below.

16. **Paired diagnostic.** Add a saved-prediction reanalysis with Ridge fixed for metadata and metadata+HSV. Pair all 96 specimen/split keys, average two absolute errors per specimen, and then weight the 48 specimens equally. HSV improves 19 and worsens 29; mean change is +0.000218646 kN and median +0.009439030 kN. Positive means worse with HSV. These are descriptive results after selection, not independent repeats, a significance test, or equivalence evidence. Their weighting differs from the primary mean of fold scores.
17. **Information availability.** Make terminal-image scoring explicit in both v2 abstracts. Add an input-timing table and identify failure-surface cover as a field whose pre-test availability is unverified. Do not turn that uncertainty into a claim that it was definitely unavailable. Verify actual test counts: four/five per within-mesh fold and eight/nine per post-onset fold. Label the classical 159-image/ten-specimen test count as reconstructed from the saved IDs and split definition.
18. **Revision and page economy.** Keep separate v1/v2 source trees. V2 adds paired errors, a compact article control table, short list captions, compact chapter headings, and explicit literature positioning. Move the repeated threshold-count table to the thesis appendix while retaining the main figure. Use IEEEtran with automatic final-column balancing; do not change its font size or margins to impose an invented page limit. The final thesis changes from 44 to 37 pages; the article remains five pages with substantive content on its final page.
19. **Visual QA correction shared by drafts.** Full-resolution inspection caught an incorrect hard-coded load-axis range in the newly generated confounding figure. Replace it with common limits derived from all 48 observed values (1.60–2.87 kN), check every plotted point is inside the axes, and retain the original correlations. Also clarify acquisition arrows, move the residual-axis scale into its label, and separate the paired-plot annotation from the last point. Because figures are shared, recompile both v1 and v2 with these correctness fixes; preserve the pre-revision v1 prose and the original committed drafts in Git. The final package must not knowingly retain a misleading plot to preserve draft appearance.
20. **Verification boundary.** Retain 43 claim groups, 72 source hashes, 60 split checks, a 181-line numerical inventory, complete final LaTeX logs, PDF/figure hashes, rendered-PDF hashes, and the report environment. Numeric matching is a lint aid, supplemented by source-specific checks and manual reading. Rendering and compilation alone do not prove scientific validity. The current historical modelling defects remain documented and unrepaired.
21. **Delivery status.** All requested report artifacts are completed locally. The remaining scientific limitations require new analysis or data and are stated in both documents; they are not unfinished report deliverables. Recommend the revised article as the first read, followed by the thesis and critique response. No remote publication, push, merge, or model training was performed.
22. **PDF navigation and log checking.** A final full-log audit found duplicate page anchors between the unnumbered thesis title and abstract. Disable only the title-page anchor in both thesis masters. Extend validation to PDF-engine warnings and normalize trailing whitespace in saved logs. A fresh render confirms all 91 page images remain identical after this navigation fix. This modifies the shared presentation of v1, not its prose, results, or review history.

## v3: figure audit and integration

23. **Preserve the completed rebuild.** Work starts from `c5a4f1a` on a new `report-rebuild-codex-v3` branch. Existing manuscript sources/PDFs, shared figure binaries, tables and evidence are hashed before edits. Only the shared provenance ledger is extended; all original ledger entries remain intact. Original reports, data, source generations and `emiling/` remain read-only.
24. **Audit the actual inventory.** There are 30 candidates, including six (not five) top-level `main_4` files. View all 28 PNGs and all 13 PDF pages. Plot-only replay from saved tables is allowed; model fitting is not. Eighteen plots were replayed, fifteen with identical pixels; nonidentical replay and unavailable montage recipes are explicitly recorded.
25. **Select three complementary diagnostics.** Consolidate four Ridge source files into a terminal parity/residual composite, full-fit coefficient magnitudes and a saved learning curve. Reject 26 candidates, most commonly because they repeat existing evidence. Coefficients are encoding-dependent descriptive summaries, not causal effects; two-repeat prediction SD is not calibrated uncertainty. Do not include partial dependence over unobserved campaign/mesh combinations.
26. **Keep generation and aggregation boundaries explicit.** The older `main_3` feature-alignment correlations are weak, unlike the later `main_4` near-identity result. Clarify that scope in v3 without changing the verified v2 claim. Averaging predictions before absolute errors produces a different diagnostic from averaging absolute errors or averaging fold metrics; retain the original primary MAE and distinguish these operations.
